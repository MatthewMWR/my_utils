server {
    listen 8080;
    root /usr/share/nginx/html;
    index index.html;

    # Proxy Prometheus API so the dashboard can query same-origin
    location /api/v1/ {
        proxy_pass http://prometheus:9090/api/v1/;
    }
}
